# advance-data-base-project
This project is a Point of Sale (POS) Database System developed using SQL Server. It manages products, customers, sales transactions, and stock automatically.
The system stores and handles all business operations like adding products, managing customers, recording sales, and updating inventory. It uses stored procedures for secure data operations and triggers to maintain stock automatically.
